import { z } from "zod";export const syncPayloadSchema = z.object({  lastSync: z.string().datetime().nullable(),  deviceId: z.string().uuid(),  changes: z.object({    clients: z.array(z.record(z.unknown())).optional(),    jobsites: z.array(z.record(z.unknown())).optional(),    quotes: z.array(z.record(z.unknown())).optional(),    quoteItems: z.array(z.record(z.unknown())).optional(),    deletes: z      .array(z.object({ table: z.string(), id: z.string() }))      .optional(),  }),});export type SyncPayloadInput = z.infer<typeof syncPayloadSchema>;