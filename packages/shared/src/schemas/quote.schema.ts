import { z } from "zod";export const quoteItemSchema = z.object({  description: z.string().min(1),  width: z.number().nullable(),  height: z.number().nullable(),  unit: z.string().min(1),  finish: z.string().nullable(),  qty: z.number().positive(),  cost: z.number().min(0),  price: z.number().min(0),});export const quoteSchema = z.object({  clientId: z.string().uuid(),  jobSiteId: z.string().uuid().nullable(),  status: z.enum(["DRAFT", "SENT", "APPROVED", "REJECTED"]),  items: z.array(quoteItemSchema).min(1),});export type QuoteInput = z.infer<typeof quoteSchema>;export type QuoteItemInput = z.infer<typeof quoteItemSchema>;