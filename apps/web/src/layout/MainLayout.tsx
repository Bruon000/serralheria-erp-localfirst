import { useState } from "react";import { Box } from "@mui/material";import { Sidebar } from "./Sidebar";import { Topbar } from "./Topbar";const SIDEBAR_WIDTH = 280;const SIDEBAR_COLLAPSED = 72;export function MainLayout({ children }: { children: React.ReactNode }) {  const [open, setOpen] = useState(true);  return (    <Box sx={{ display: "flex", minHeight: "100vh" }}>      <Sidebar open={open} onToggle={() => setOpen(!open)} width={SIDEBAR_WIDTH} collapsedWidth={SIDEBAR_COLLAPSED} />      <Box        component="main"        sx={{          flexGrow: 1,          width: open ? `calc(100% - ${SIDEBAR_WIDTH}px)` : `calc(100% - ${SIDEBAR_COLLAPSED}px)`,          transition: "width 0.2s",          display: "flex",          flexDirection: "column",        }}      >        <Topbar />        <Box sx={{ flex: 1, p: 3, overflow: "auto" }}>{children}</Box>      </Box>    </Box>  );}