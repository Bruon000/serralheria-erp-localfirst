import { useState } from "react";import { useQuery } from "@tanstack/react-query";import { Box, Button, Paper, Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Typography } from "@mui/material";import { Add } from "@mui/icons-material";import { useNavigate } from "react-router-dom";import { apiJson } from "@/api/client";import { quotes as quotesUrl } from "@/api/endpoints";import { QuoteFormDrawer } from "./QuoteFormDrawer";export function QuoteList() {  const navigate = useNavigate();  const [drawerOpen, setDrawerOpen] = useState(false);  const { data: list = [], refetch } = useQuery({    queryKey: ["quotes"],    queryFn: () => apiJson<any[]>(quotesUrl),  });  return (    <Box>      <Box sx={{ display: "flex", justifyContent: "space-between", alignItems: "center", mb: 2 }}>        <Typography variant="h5">Orçamentos</Typography>        <Button variant="contained" startIcon={<Add />} onClick={() => setDrawerOpen(true)}>          Novo orçamento        </Button>      </Box>      <TableContainer component={Paper}>        <Table>          <TableHead>            <TableRow>              <TableCell>Cliente</TableCell>              <TableCell>Status</TableCell>              <TableCell>Total</TableCell>              <TableCell align="right">Ações</TableCell>            </TableRow>          </TableHead>          <TableBody>            {list.map((row: any) => (              <TableRow key={row.id} hover onClick={() => navigate(`/quotes/${row.id}`)} sx={{ cursor: "pointer" }}>                <TableCell>{row.client?.name ?? row.clientId}</TableCell>                <TableCell>{row.status}</TableCell>                <TableCell>{Number(row.total ?? 0).toLocaleString("pt-BR")}</TableCell>                <TableCell align="right" onClick={(e) => e.stopPropagation()}>                  <Button size="small" onClick={() => navigate(`/quotes/${row.id}`)}>                    Ver                  </Button>                </TableCell>              </TableRow>            ))}          </TableBody>        </Table>      </TableContainer>      <QuoteFormDrawer open={drawerOpen} onClose={() => setDrawerOpen(false)} onSaved={() => refetch()} />    </Box>  );}