import { useState } from "react";import { useQuery } from "@tanstack/react-query";import {  Box,  Button,  Paper,  Table,  TableBody,  TableCell,  TableContainer,  TableHead,  TableRow,  Typography,} from "@mui/material";import { Add } from "@mui/icons-material";import { apiJson } from "@/api/client";import { jobsites as jobsitesUrl } from "@/api/endpoints";import { JobSiteFormDrawer } from "./JobSiteFormDrawer";export function JobSiteList() {  const [drawerOpen, setDrawerOpen] = useState(false);  const { data: list = [], refetch } = useQuery({    queryKey: ["jobsites"],    queryFn: () => apiJson<any[]>(jobsitesUrl),  });  return (    <Box>      <Box sx={{ display: "flex", justifyContent: "space-between", alignItems: "center", mb: 2 }}>        <Typography variant="h5">Obras</Typography>        <Button variant="contained" startIcon={<Add />} onClick={() => setDrawerOpen(true)}>          Nova obra        </Button>      </Box>      <TableContainer component={Paper}>        <Table>          <TableHead>            <TableRow>              <TableCell>Nome</TableCell>              <TableCell>Cliente</TableCell>              <TableCell>Endereço</TableCell>            </TableRow>          </TableHead>          <TableBody>            {list.map((row: any) => (              <TableRow key={row.id}>                <TableCell>{row.name}</TableCell>                <TableCell>{row.client?.name ?? "—"}</TableCell>                <TableCell>{row.address ?? "—"}</TableCell>              </TableRow>            ))}          </TableBody>        </Table>      </TableContainer>      <JobSiteFormDrawer open={drawerOpen} onClose={() => setDrawerOpen(false)} onSaved={() => refetch()} />    </Box>  );}