import { Injectable } from "@nestjs/common";import { PrismaService } from "../prisma/prisma.service";@Injectable()export class WorkflowsService {  constructor(private prisma: PrismaService) {}  async findAll(companyId: string) {    return this.prisma.workflow.findMany({      where: { companyId },      include: { states: true, transitions: true },      orderBy: { name: "asc" },    });  }  async findOne(companyId: string, id: string) {    return this.prisma.workflow.findFirst({      where: { id, companyId },      include: { states: true, transitions: { include: { fromState: true, toState: true } } },    });  }}