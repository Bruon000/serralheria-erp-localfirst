import { Controller, Get, UseGuards } from "@nestjs/common";import { JwtAuthGuard } from "../auth/guards/jwt-auth.guard";@Controller("integrations")@UseGuards(JwtAuthGuard)export class IntegrationsController {  @Get("fiscal")  getFiscalConfig() {    return { provider: null, apiKey: null, message: "Configuração fiscal (esqueleto)" };  }  @Get("contador")  getContadorExport() {    return { message: "Exportação contador (placeholder)" };  }}