import { Controller, Get, Query, UseGuards, Request } from "@nestjs/common";import { ActivityFeedService } from "./activity-feed.service";import { JwtAuthGuard } from "../auth/guards/jwt-auth.guard";@Controller("activity-feed")@UseGuards(JwtAuthGuard)export class ActivityFeedController {  constructor(private service: ActivityFeedService) {}  @Get()  findRecent(@Request() req: any, @Query("limit") limit?: string) {    return this.service.findRecent(req.user.companyId, limit ? parseInt(limit, 10) : 20);  }}