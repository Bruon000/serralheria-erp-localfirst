import { Injectable } from "@nestjs/common";import { PrismaService } from "../prisma/prisma.service";@Injectable()export class ActivityFeedService {  constructor(private prisma: PrismaService) {}  async findRecent(companyId: string, limit = 20) {    return this.prisma.activityEvent.findMany({      where: { companyId },      include: { user: { select: { id: true, name: true } } },      orderBy: { createdAt: "desc" },      take: limit,    });  }  async create(companyId: string, userId: string, type: string, entityType: string, entityId: string, payload?: any) {    return this.prisma.activityEvent.create({      data: { companyId, userId, type, entityType, entityId, payload },    });  }}